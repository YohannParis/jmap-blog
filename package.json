{
  "name": "poem-blog",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm install fs-extra && node blog-generator.js",
    "check-email": "npx --yes --package imap --package mailparser --package @octokit/rest --package dotenv -- node utils/check-email.js",
    "format": "npx prettier --write .  && npx prettier \"./templates/*.kit\" --write --parser html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "fs-extra": "^11.3.0"
  }
}
